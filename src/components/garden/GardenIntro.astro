---
import PageHero from '../PageHero.astro';

interface Props {
  title: string;
  description: string;
}
const { title, description } = Astro.props;
---

<PageHero
  title={title}
  description={description}
/>
