---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<p class:list={[
  "text-base sm:text-lg md:text-xl lg:text-2xl text-stone-600",
  className
]}>
  <slot />
</p>
