---
import { Picture } from 'astro:assets';
import { getMaturityIcon } from '../utils/maturityIcons';
import { resolveImage } from '../utils/resolveImage';
import PageHero from './PageHero.astro';
import { pageType } from '../styles/typography';


interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  heroImage?: string;
  tags: string[];
  maturity?: 'seed' | 'plant' | 'tree';
}

const { title, description, pubDate, updatedDate, heroImage, tags = [], maturity } = Astro.props;
const maturityIcon = getMaturityIcon(maturity);
// LEARN: <Picture> emits AVIF + WebP <source>s for the hero (best compression on a
// large above-the-fold image). Remote heroes (e.g. the series imgur image) resolve
// to undefined → passthrough <img>.
const heroImg = resolveImage(heroImage);

const formattedDate = pubDate.toLocaleDateString('en-us', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
});
const formattedUpdatedDate = updatedDate?.toLocaleDateString('en-us', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
});
---

<PageHero title={title} description={description} titleClass="p-name" descriptionClass="p-summary" class="justify-start mb-10">
  <!-- Metadata row -->
  <div class={`${pageType.date} flex flex-wrap items-center gap-x-4 gap-y-1 text-syoro/60 mb-4 mt-10`}>
    {maturityIcon && (
      <div class="flex items-center gap-1.5">
        <img src={maturityIcon} class="w-4 opacity-60" alt="" />
        <span>{maturity}</span>
      </div>
    )}
    <time datetime={pubDate.toISOString()} class="dt-published">{formattedDate}</time>
    {formattedUpdatedDate && (
      <span>· Updated {formattedUpdatedDate}</span>
    )}
  </div>

  <!-- Tags -->
  {tags.length > 0 && (
    <div class="flex flex-wrap items-center gap-x-1 text-lg font-sans text-syoro/60">
      {tags.map((tag, index) => (
        <>
          <a
            href={`/tags/${tag.toLowerCase()}`}
            class="hover:text-syoro/80 transition-colors duration-200 hover:italic"
          >
            {tag.charAt(0).toUpperCase() + tag.slice(1)}
          </a>
          {index < tags.length - 1 && <span>·</span>}
        </>
      ))}
    </div>
  )}

  <!-- Hero image, clean and simple -->
  {heroImage && (heroImg ? (
    <Picture
      src={heroImg}
      formats={['avif', 'webp']}
      fallbackFormat="webp"
      alt=""
      class="w-screen mt-12 rounded mx-auto"
      loading="eager"
    />
  ) : (
    <img
      src={heroImage}
      alt=""
      class="w-screen mt-12 rounded mx-auto"
      loading="eager"
    />
  ))}
</PageHero>
