# CLAUDE.md — ruwaizrazak.com

All project rules — stack, conventions, styling, Astro/Svelte boundaries, SEO, key files, testing, Plan Drift Lessons — live in `AGENTS.md`, imported below so Claude and Codex read the same text. **Edit shared rules in `AGENTS.md`, never here.**

The Codex workflow (orchestrator runbook and executor contract) is global, imported from `~/.claude/CLAUDE.md`; this repo's checks, protected paths and branch policy are in `.agents/profile.sh`.

@AGENTS.md
